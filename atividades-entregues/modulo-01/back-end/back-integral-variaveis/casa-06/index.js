// Calculando a velocidade média.
const distPercorrida = 8000,
    tempo = 1800;

const velocidade = ((distPercorrida / tempo) * 3.6).toFixed(2);

console.log(`A sua velocidade média é ${velocidade} km/h`);