// infinity e NaN
let x = 10,
    y = 0;

// Divisão por zero = infinity
let resultado = x / y;
console.log(`A divisão de ${x} e ${y} é ${resultado}`);


// zero por zero
x = 0;
resultado = x / y;
console.log(`A divisão de ${x} e ${y} é ${resultado}`);