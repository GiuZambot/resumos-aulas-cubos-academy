// Exerccício três minha solução
let x = 10,
    y = 2,
    concat = "";

// Somar
let resultado = x + y;
concat += `A soma de ${x} e ${y} é ${resultado} \n`;

// Subtrair
resultado = x - y;
concat += `A subtração de ${x} e ${y} é ${resultado} \n`;

// Multiplicar
resultado = x * y;
concat += `A multiplicação de ${x} e ${y} é ${resultado} \n`;

// Dividir
resultado = x / y;
concat += `A divisão de ${x} e ${y} é ${resultado} \n`;

// Exponenciar 
resultado = x ** y;
concat += `A exponenciação de ${x} e ${y} é ${resultado} \n`;

// Radiaciação
resultado = x ** (1 / y);
concat += `A radiaciação de ${x} e ${y} é ${resultado} \n`;
console.log(concat);


// Resolução do Vídeo
// Somar
resultado = x + y;
console.log("A soma de " + x + " e " + y + " é " + resultado);

// Subtrair
resultado = x - y;
console.log("A subtração de " + x + " e " + y + " é " + resultado);


// Multiplicar
resultado = x * y;
console.log("A multiplicação de " + x + " e " + y + " é " + resultado);


// Dividir
resultado = x / y;
console.log("A divisão de " + x + " e " + y + " é " + resultado);

// Exponenciar 
resultado = x ** y;
console.log("A exponenciação de " + x + " e " + y + " é " + resultado);

// Radiaciação
resultado = x ** (1 / y);
console.log("A radiaciação de " + x + " e " + y + " é " + resultado + ' \n');

//Solução Vídeo com string template
// Somar
resultado = x + y;
console.log(`A soma de ${x} e ${y} é ${resultado}`);

// Subtrair
resultado = x - y;
console.log(`A subtração de ${x} e ${y} é ${resultado}`);

// Multiplicar
resultado = x * y;
console.log(`A multiplicação de ${x} e ${y} é ${resultado}`);

// Dividir
resultado = x / y;
console.log(`A divisão de ${x} e ${y} é ${resultado}`);

// Exponenciar 
resultado = x ** y;
console.log(`A exponenciação de ${x} e ${y} é ${resultado}`);

// Radiaciação
resultado = x ** (1 / y);
console.log(`A radiaciação de ${x} e ${y} é ${resultado}`);